.model small

.stack

.data
	b DW ?
	T0 DW ?
	T1 DW ?

.code
begin:
	mov ax, @data
	mov ds, ax

	mov ax, 2
	mov bx, 5
	div bx
	mov T0, ax

	mov ax, T0
	mov T1, ax
	mov ax, T1
	mov b, ax

	END begin
